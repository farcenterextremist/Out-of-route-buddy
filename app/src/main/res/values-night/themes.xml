<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.OutofRoutebuddy" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Widget control colors: kills all teal -->
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="colorControlHighlight">#20FFFFFF</item>
        <item name="android:colorControlActivated">@color/accent</item>
        <item name="android:colorControlHighlight">#20FFFFFF</item>
        <!-- Background color -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/background</item>
        <!-- Enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <!-- Switch widget style override -->
        <item name="switchStyle">@style/OORBSwitch</item>
        <item name="switchPreferenceCompatStyle">@style/OORBSwitchPreference</item>
        <!-- Explicit text colors for dark mode (fixes invisible preference text) -->
        <item name="android:textColorPrimary">@color/text_primary_adaptive</item>
        <item name="android:textColorSecondary">@color/text_secondary_adaptive</item>
        <!-- Preferences: force accent into preference widgets -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.OORB</item>
        <!-- Menu styling for dark mode -->
        <item name="android:popupMenuStyle">@style/PopupMenuStyle</item>
    </style>

    <!-- Menu style for dark mode -->
    <style name="PopupMenuStyle" parent="Widget.MaterialComponents.PopupMenu">
        <item name="android:popupBackground">@color/surface</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

</resources>










