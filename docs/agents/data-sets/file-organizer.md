# File Organizer — data set

## Consumes (reads / references)

- **Repo tree:** Top-level folders (`app/`, `docs/`, `scripts/`, `phone-emulator/`, etc.); naming patterns. No deep code.

## Produces (writes / owns)

- `docs/agents/FILE_STRUCTURE.md` or similar; reorg proposals (in docs). No moving files without coordinator/user approval.

## Delegation

Propose docs/ structure (e.g. product vs agents vs ops); add docs index and link key plans; decide where WORKER_TODOS and future plans live.
