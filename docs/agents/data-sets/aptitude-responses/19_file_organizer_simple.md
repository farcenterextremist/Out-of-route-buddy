# File Organizer — Simple

- **Role card:** `docs/agents/roles/file-organizer.md`
- **Prompt type:** Simple

## Prompt (copy-paste)

```
Where do agent role definitions live in this repo (folder path)?
```

## Look for (scoring)

docs/ structure; no content authoring; handoff to Coordinator or user for approval.

---

## Response

Agent role definitions live in **`docs/agents/roles/`** (one `.md` per role, e.g. `frontend-engineer.md`, `red-team-agent.md`). Coordinator instructions are in **`docs/agents/coordinator-instructions.md`**. I don’t author content; structure and naming—hand off to Coordinator or user for approval.

